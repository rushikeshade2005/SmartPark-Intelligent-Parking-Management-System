# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb+srv://rushikeshade2005_db_user:<db_password>@cluster0.yoi5r3y.mongodb.

# JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# Client URL (for CORS and email links)
CLIENT_URL=http://localhost:5173

# Email Configuration (SMTP)
# For development, leave blank to use Ethereal (auto-generated test account)
# For production, use your SMTP provider (Gmail, SendGrid, Mailgun, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@smartpark.com
FROM_NAME=SmartPark
